apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: aiplayground-tls
  namespace: aiplayground
spec:
  secretName: aiplayground-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - aiplayground.gcp.jknowledge.se
